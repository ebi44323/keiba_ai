# keiba-ebye 引継ぎ文書 2026-04-07

## プロジェクト概要
競馬AI予想システム。Streamlit + LightGBM 3モデルアンサンブル。
GitHub push → HuggingFace Spaces に自動デプロイ。

---

## 2026-04-07 実施した変更（本セッション）

### 1. 馬場適性特徴量の追加（再学習必須）
- `馬_重馬場_着順パーセント`: 馬個別の重/不良馬場 着順パーセント expanding mean
- `父_重馬場_着順パーセント`: 種牡馬産駒の重/不良馬場 平均着順パーセント
- inference用の lookup dict: `horse_heavy_dict` / `sire_heavy_dict` を bundle に追加
- **背景**: 馬場変更（良→不良）が予想に反映されていなかった。LGBMRankerは同一レース内で全馬同値の特徴量を無視するため、「馬ごとに異なる重馬場実績」を追加した

### 2. サイドバー馬場設定を競馬場別に対応
- 「競馬場別に設定する」チェックボックス追加
- 複数会場で馬場が異なる日に会場ごとに芝/ダートを個別設定可能
- `baba_override` の形式:
  - 従来: `{'芝': '不良', 'ダート': '重'}` (全会場共通)
  - 新形式: `{'東京': {'芝': '不良', 'ダート': '重'}, '阪神': {'芝': '重'}}` (会場別)
  - `inference.py` で両形式を自動判定

### 3. 馬券メモを auto_morning.py HTML/TXT 出力に反映
- `load_horse_memos()` 追加（ローカル→GitHub APIフォールバック）
- HTML: メモあり馬に紫左ボーダー + 📝バッジ、テーブル下に馬券メモセクション
- TXT: 末尾に【メモあり馬】セクション追加

### 4. 調教評価スコア（Phase1 + Phase2 同時実装）

#### アーキテクチャの重要な決定
調教データを **モデル特徴量として使わず、ポストモデルスコア補正として適用**。
理由: 歴史データに調教情報がないため訓練/推論のズレが発生するのを防ぐ。

```
モデル予測(raw_scores)
  ↓
[グレード補正] S=+0.06, A=+0.03, B=0.0, C=-0.04
  ↓
[Geminiコメント補正] コメントスコア × 0.02 (-2〜+2 → -0.04〜+0.04)
  ↓
softmax → 勝率 → 最終予想
```

- **バックテスト時**: 補正ゼロ（`_oikiri_data={}` のため）→ 振り返りと実予想で齟齬なし
- **実予想時**: 前日13時以降に公開された評価を自動取得して補正

#### 実装ファイル
| ファイル | 変更内容 |
|---|---|
| `src/scraper.py` | `fetch_oikiri_data(race_id)` 追加: 評価(S/A/B/C) + コメントを取得 |
| `src/gemini_utils.py` | `score_oikiri_comments(oikiri_data)` 追加: コメントをバッチスコアリング |
| `src/inference.py` | アンサンブル後にポストモデル補正 + `調教評価` 列を `df_test` に追加 |
| `app.py` | 出馬表に `調教評価` 列を表示（動作確認用） |

#### 動作確認方法
- **金曜13時以降** に任意のレースを予想実行
- 出馬表の「調教評価」列に A/B/C/S が表示されれば取得成功
- 全馬空欄 or '-' → まだ未公開（木曜以前は正常）

#### スクレイピング技術的詳細
- URL: `https://race.netkeiba.com/race/oikiri.html?race_id={race_id}`
- 評価(A/B/C)は静的HTML内に存在（ログイン不要）
- コメントは動的ロード（AJAX）の可能性が高い。取得できた場合のみGemini処理
- `@st.cache_data(ttl=3600)` でキャッシュ済み

---

## 現在の特徴量数
- NUM_FEATURES: **53特徴量**（馬場適性2本追加、調教評価はポストモデルのため含まず）
- CAT_FEATURES: 13特徴量（変更なし）

---

## 再学習が必要な変更（本セッション）
- 馬場適性特徴量（`馬_重馬場_着順パーセント`, `父_重馬場_着順パーセント`）を追加
- → `train_and_push.py` の実行 or GitHub Actions「Retrain and Push Model」が必要

---

## 現在の未解決課題

### 調教コメント取得（Phase2）
- 現状: コメントは動的ロード（AJAX）のため、requests+BSでは取得できない可能性
- Gemini処理は「コメントが取得できた場合のみ」実行される
- 将来的改善案:
  1. netkeibaプレミアム会員化でラップタイムも取得
  2. Playwright/Seleniumで動的コンテンツも取得（GitHub Actionsでは利用可能）
  3. AJAX エンドポイントの特定（要調査）

### 調教の歴史データ
- 学習データ(learning_data_perfect_tier.csv)に調教データなし
- 将来的には netkeiba の歴史調教ページを遡って収集→再学習でモデルに組み込み可能
- 遡及スクレイピングには追加スクリプトが必要

### AIスコアキャリブレーション
- 本命AI勝率 ~22% vs 実際の勝者AI勝率 ~13-14%（過信気味）
- 単勝回収率のバラつきと関連
- 継続モニタリング中

---

## アンサンブル重みとモデルパラメータ（最新）
- wa=0.0581 / wb=0.8159 / wc=0.1261 (2026-03-30 最適化)
- モデルB（1着予測Ranker）: n_estimators=685, learning_rate=0.029074, num_leaves=15 (2026-04-03 Optuna)

---

## Discord 自動通知（土日稼働中）
| トリガー | 内容 |
|---|---|
| 土日 8:00 | 全レース予想 HTML+TXT → Discord |
| 土日 9:00〜16:45（15分ごと） | 発走前10〜60分のレースを自動予想 |
| 土日 17:00 | 当日振り返り |
| 日曜 20:00 | 土日合算まとめ |

---

## 次にやること（優先度順）
1. **push → 再学習**（馬場適性特徴量追加のため必須）
2. **今週末に調教評価の動作確認**（金曜13時以降に予想実行して「調教評価」列を確認）
3. **コメント取得の可否確認**（上記確認時にコメント列も見る）
4. **数週間後: 重馬場レースでの予想精度確認**（2026-04-07の主な目的）
5. **将来: 調教歴史データ収集 → モデルに完全組み込み**

---

## よく使うコマンド
```powershell
# ローカル再学習
$env:HF_TOKEN="hf_xxxx"
$env:HF_REPO_ID="ebi44323/keiba-ebye-models"
python train_and_push.py

# ローカル動作確認
streamlit run app.py
```
