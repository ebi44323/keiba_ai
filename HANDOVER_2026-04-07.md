# keiba-ebye 引継ぎ文書 2026-04-07

## プロジェクト概要
競馬AI予想システム。Streamlit + LightGBM 3モデルアンサンブル。
GitHub push → HuggingFace Spaces に自動デプロイ。

---

## 2026-04-07 実施した変更（本セッション全体）

### 1. 馬場適性特徴量の追加（再学習済み）
- `馬_重馬場_着順パーセント`: 馬個別の重/不良馬場 着順パーセント expanding mean
- `父_重馬場_着順パーセント`: 種牡馬産駒の重/不良馬場 平均着順パーセント
- inference用 lookup dict: `horse_heavy_dict` / `sire_heavy_dict` を bundle に追加
- **背景**: LGBMRankerは同一レース内で全馬同値の特徴量を無視するため、馬ごとに異なる重馬場実績を追加した
- **再学習**: 済み（ユーザーが実施）

### 2. サイドバー馬場設定を競馬場別に対応
- 「競馬場別に設定する」チェックボックス追加
- `baba_override` の形式:
  - 従来: `{'芝': '不良', 'ダート': '重'}` (全会場共通)
  - 新形式: `{'東京': {'芝': '不良', 'ダート': '重'}, '阪神': {'芝': '重'}}` (会場別)
  - `inference.py` で両形式を自動判定

### 3. 馬券メモを auto_morning.py HTML/TXT 出力に反映
- `load_horse_memos()` 追加（ローカル→GitHub APIフォールバック）
- HTML: メモあり馬に紫左ボーダー + 📝バッジ
- TXT: 末尾に【メモあり馬】セクション追加

### 4. 調教評価スコア（Phase1 + Phase2 完全実装）

#### アーキテクチャの重要な決定
調教データを **モデル特徴量として使わず、ポストモデルスコア補正として適用**。
理由: 歴史データに調教情報がないため訓練/推論のズレが発生するのを防ぐ。

```
モデル予測(raw_scores)
  ↓
[グレード補正] S=+0.07, A=+0.07, B=0.0, C=-0.07
  ↓
[Geminiコメント補正] コメントスコア × 0.02 (-2〜+2 → -0.04〜+0.04)
  ↓
softmax → 勝率 → 最終予想
```

- **バックテスト時**: 補正ゼロ（`use_oikiri=None` かつ `skip_live_scrape=True` のため）
- **当日振り返り時**: `use_oikiri=True` を明示して補正あり（朝刊予想との整合性維持）
- **Optuna等長期バックテスト**: `use_oikiri=None`（スキップ）のまま

#### 補正値の根拠（実績ベース・2026-04-07 算出）
`collect_oikiri_history.py` で 2025/01〜2026/04 の 4,300レース・58,000頭分を分析:

| グレード | 頭数 | 全体比 | 1着率 | 複勝率 | 補正値 |
|---|---|---|---|---|---|
| S | 0 | 0% | — | — | +0.07 (A同値) |
| A | 691 | 1.2% | **25.0%** | 49.8% | **+0.07** |
| B | 34,994 | 60.4% | 9.8% | 28.0% | 0.0 |
| C | 22,211 | 38.3% | 3.4% | 12.6% | **−0.07** |

- Sグレードは2025年以降の実データでは観測されず（実質3段階）
- A評価馬はB馬の**2.56倍**の1着率。レアだが出たら強力
- C評価は全馬の38%と多く、マイナス補正の影響が大きい

#### 実装ファイル
| ファイル | 変更内容 |
|---|---|
| `src/scraper.py` | `fetch_oikiri_data(race_id)` 追加: 評価(S/A/B/C) + コメントを取得 |
| `src/gemini_utils.py` | `score_oikiri_comments(oikiri_data)` 追加: コメントをバッチスコアリング |
| `src/inference.py` | `use_oikiri` パラメータ追加・ポストモデル補正・`調教評価` 列を `df_test` に追加 |
| `app.py` | 出馬表に `調教評価` 列・性能詳細タブにも表示（S=金/A=緑/C=グレー） |
| `auto_review.py` | `use_oikiri=True` 追加（当日振り返りで調教補正を適用） |
| `collect_oikiri_history.py` | 過去調教データ一括収集 + 補正値分析スクリプト（新規） |

#### 動作確認方法
- **金曜13時以降** に任意のレースを予想実行
- 出馬表の「調教評価」列に A/B/C が表示されれば取得成功
- 全馬空欄 or '-' → まだ未公開（木曜以前は正常）

#### use_oikiri パラメータの設計
```python
run_real_prediction(..., use_oikiri=None)  # デフォルト: skip_live_scrapeに従う
run_real_prediction(..., use_oikiri=True)  # 強制取得（当日振り返り用）
run_real_prediction(..., use_oikiri=False) # 強制スキップ（長期バックテスト用）
```

---

## 現在の特徴量数
- NUM_FEATURES: **53特徴量**（馬場適性2本追加、調教評価はポストモデルのため含まず）
- CAT_FEATURES: 13特徴量（変更なし）

---

## 補正値の再校正手順
数ヶ月ごとに `collect_oikiri_history.py` を再実行して精度を確認・更新推奨:
```powershell
# 既存データに追記（中断再開可能）
python collect_oikiri_history.py

# 既存 oikiri_history.csv を分析のみ
python collect_oikiri_history.py --analyze
```
結果の「推奨補正値」をもとに `inference.py` の `_GRADE_BOOST` を更新する。

---

## アンサンブル重みとモデルパラメータ（最新）
- wa=0.0581 / wb=0.8159 / wc=0.1261 (2026-03-30 最適化)
- モデルB（1着予測Ranker）: n_estimators=685, learning_rate=0.029074, num_leaves=15 (2026-04-03 Optuna)

---

## Discord 自動通知（土日稼働中）
| トリガー | 内容 |
|---|---|
| 土日 8:00 | 全レース予想 HTML+TXT → Discord |
| 土日 9:00〜16:45（15分ごと） | 発走前10〜60分のレースを自動予想 |
| 土日 17:00 | 当日振り返り（調教評価の補正込み） |
| 日曜 20:00 | 土日合算まとめ |

---

## 現在の未解決課題

### 調教コメント取得（Phase2）
- 現状: コメントは動的ロード（AJAX）のため、requests+BSでは取得できない可能性
- Gemini処理は「コメントが取得できた場合のみ」実行される
- ログに `調教コメントスコア生成完了` が出れば取得成功
- 将来的改善案:
  1. Playwright/Seleniumで動的コンテンツも取得（GitHub Actionsでは利用可能）
  2. AJAX エンドポイントの特定（要調査）
  3. netkeibaプレミアム会員化でラップタイムも取得

### AIスコアキャリブレーション
- 本命AI勝率 ~22% vs 実際の勝者AI勝率 ~13-14%（過信気味）
- 継続モニタリング中

---

## ★ 今後の改善提案（優先度順）

### 【高優先度】精度向上に直結するファクター

#### 1. 調教タイム・ラップの取得（netkeibaプレミアム）
現在取得できているのは評価(A/B/C)のみ。プレミアム会員になると：
- 坂路タイム（4F・3F・1F）
- CW・南W等コース別タイム
- 本数・強度（馬なり/一杯）
これらはモデル特徴量として直接使えるため**最も効果的な次の一手**。
月額数百円 vs 予想精度向上の費用対効果は高い。

#### 2. 調教評価の歴史データをモデル特徴量に組み込む
現在のA/B/Cは「ポストモデル補正」止まり。
`collect_oikiri_history.py` で2025年分の調教データは収集済みなので：
- `oikiri_history.csv` を学習データに結合
- `調教評価スコア` を NUM_FEATURES に追加して再学習
→ モデルが調教パターンを直接学習できるようになる
→ 補正値の恣意性もなくなる

#### 3. 馬体重変化のトレンド特徴量
現在は「馬体重変化量」だけだが、方向性のトレンドが重要:
- 直近3走で増加傾向 / 減少傾向 / 安定 のフラグ
- 成長期（3歳前半）の増加は好材料
- 急激な増減（前走比±10kg超）は要注意フラグ

#### 4. 前日最終オッズと単勝オッズの乖離
前日夜の最終オッズ vs レース当日の最終オッズの変化量:
- 当日急に人気上昇 → 陣営情報が漏れている可能性
- 前日から大きく下がっている馬（売れてきている）
netkeiba前日オッズをスクレイピングして特徴量化できる。

---

### 【中優先度】情報精度向上

#### 5. 天気予報との連動（馬場状態の予測精度向上）
現在は手動で馬場状態を設定しているが：
- 天気予報APIを使って当日の降水確率・降水量を取得
- 馬場悪化リスクを自動推定してサイドバーに表示
- 「本日の降水確率60%→重〜不良になる可能性あり」などの警告表示

#### 6. パドック評価の取得
netkeibaのパドックコーナーに記者の短評が載ることがある:
- GeminiでA/B/C化してポストモデル補正に追加
- 調教評価と同じアーキテクチャで実装可能

#### 7. 騎手の直近フォーム（乗り替わり効果）
現在は「騎手×競馬場」「騎手×距離」のTE特徴量のみ。
ユーザー方針で除外しているが、以下は後発情報にならない:
- 騎手の直近10鞍の着順パーセント（状態把握）
- 乗り替わり時に「前騎手より上位/下位か」の指標
（※現行の乗り替わりフラグは0/1のみ）

---

### 【低優先度】長期的な精度改善

#### 8. コース傾向の詳細化（枠番有利不利）
競馬場×距離×枠番の有利不利は実績から学習できる:
- 学習データには競馬場・枠番・着順が既にある
- expanding mean で「この枠番でこの競馬場の勝率」を特徴量化
- 特に芝1200m・ダート1400m等は枠番依存が強い

#### 9. AIキャリブレーション改善
本命AI勝率~22% vs 実際勝者の平均AI勝率~13-14%のギャップ解消:
- Platt Scaling の再調整（学習データが増えたため再実施推奨）
- または softmax temperature を調整（現在のスケールを確認）

#### 10. 季節性・開催週の特徴量
- 開催何週目かのフラグ（馬場悪化を近似）
- 春/秋/夏競馬の区別（特に夏の洋芝は別物）
- 重賞前週は強い馬が回避するため出走馬レベルが下がる傾向

#### 11. 長期的: 競馬ブックスマートの調教データ活用
現在ユーザーが目視で確認しているデータをシステムに取り込む:
- 競馬ブックは調教評価の精度が高い（記者による)
- スクレイピング技術的難易度は高め
- まずnetkeibaプレミアムで実績を積んでから検討

---

## 補正値・パラメータ一覧（最新）

### 調教評価補正値（2026-04-07 4,300レース実績から算出）
```python
_GRADE_BOOST = {'S': 0.07, 'A': 0.07, 'B': 0.0, 'C': -0.07}
```

### Geminiコメントスコア補正係数
```python
× 0.02  # スコア-2〜+2 → raw_score -0.04〜+0.04
```

### アンサンブル重み
```python
wa=0.0581 / wb=0.8159 / wc=0.1261
```

---

## よく使うコマンド
```powershell
# ローカル再学習
$env:HF_TOKEN="hf_xxxx"
$env:HF_REPO_ID="ebi44323/keiba-ebye-models"
python train_and_push.py

# ローカル動作確認
streamlit run app.py

# 調教評価 過去データ収集（夜間実行推奨 約2時間）
python collect_oikiri_history.py

# 調教評価 分析のみ（収集済みデータから）
python collect_oikiri_history.py --analyze
```

---

## 次にやること（優先度順）
1. **今週末に調教評価の動作確認**（金曜13時以降に予想実行→「調教評価」列を確認）
2. **Geminiコメント取得の可否確認**（ログに `調教コメントスコア生成完了` が出るか）
3. **数週間後: 重馬場レースでの予想精度確認**（馬場適性特徴量の効果検証）
4. **collect_oikiri_history.py の定期実行**（月1回程度、補正値の再校正）
5. **中期: netkeibaプレミアム検討**（調教タイム取得 → 最大の精度向上余地）
6. **中期: 調教評価をモデル特徴量に組み込む**（oikiri_history.csv + 再学習）
