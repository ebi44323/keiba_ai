# keiba-ebye 引継ぎ資料 2026-03-26

## 現在の状況（作業中断時点）

### HF Space ビルド状況
- **最新コミット（GitHub）**: `02f1e94` — `app.py` caption `v2026-03-26d`
- **HF Spaceの最新コミット**: `758b4f1` — "Deploy from GitHub"（sync.yml経由）
- **ビルド状態**: Building中（No logs表示 → 正常。旧コンテナが稼働しながら新ビルドが裏で走っている）
- **するべきこと**: ビルド完了を待つだけ。完了すると一瞬白くなり `v2026-03-26d` が表示されるはず

### デプロイの仕組み（重要）
HF Spacesはゼロダウンタイムデプロイ：
- 旧コンテナが動き続ける → アプリが使える・No logs
- 新ビルドがバックグラウンドで完了
- 完了時に切り替え（一瞬白くなる）
- ビルドログは HF > Logs > **Build** タブで確認

---

## 今セッションで実装した内容（完了済み）

### 1. Gemini AI 二人アナリスト（`src/gemini_utils.py`）
- **伊藤ホンメ**（本命党）: AI最高勝率馬を軸に展開推論。最後に「N番〇〇から入ります」
- **風穴あけるズ**（穴党）: EV1.5以上・穴馬マーク🎯を溺愛。テンション高め
- `generate_two_analysts()` → `{'honmei': {'comment', 'bet'}, 'ana': {'comment', 'bet'}, 'model': str}`
- アナリスト名は旧バージョン（鉄板師・剛三）から変更済み

### 2. Gemini 表示（`app.py` の `_render_gemini_tab`）
- `@st.fragment` デコレータで独立リラン（スライダー操作でGeminiが消えないように）
- デフォルトON: `st.session_state["gemini_mode_toggle"] = True` を明示初期化
- tab5「🤖 AI思考」タブに表示

### 3. PRO_PASSWORD メニュー（`app.py`）
- `_FREE_ACTIONS = ["⏩ 次のレースを予想", "📝 馬券メモ管理"]`（無料）
- Pro: 上記 + 振り返り・長期成績・愛馬の成長記録・Optuna・バックテスト・騎手DB等
- `PRO_PASSWORD` Secret を HF Spaces に設定すれば有効化（未設定時は全機能フリー）
- `OWNER_KEY` Secret + URLパラメータ `?key=<OWNER_KEY>` でオーナー自動ログイン

### 4. 振り返りキャッシュ修正（`app.py`）
- 振り返り結果を `st.session_state[f'review_data_{target_date}']` にキャッシュ
- Geminiボタン押下で結果が消えるバグを修正

### 5. `.streamlit/config.toml`
```toml
[server]
port = 7860
headless = true
```
HF Spaceのヘルスチェックが7860ポートを見るため必須

### 6. `sync.yml` 修正（HFデプロイの根本的修正）
```yaml
rm -rf .git
git init
git branch -M main
git config --global user.email "action@github.com"
git config --global user.name "GitHub Action"
rm -f learning_data_perfect_tier.zip
rm -f learning_data_perfect_tier.csv
rm -f trained_assets.pkl
rm -f keiba.log
rm -rf back2/ backup/ __pycache__/ src/__pycache__/
rm -f app_backup_before_refactor.py
# ...その他不要ファイル削除
git add .
git commit -m "Deploy from GitHub"
git remote add hf https://ebi44323:${{ secrets.HF_TOKEN }}@huggingface.co/spaces/ebi44323/keiba-ebye
git push --force hf main
```
**注意**: `pedigree_master_all.csv`（3.14MB）は削除しないこと。アプリが必要とする。

---

## HF Space が壊れたときの対処法（重要）

### 症状: LFS大容量ファイルが HF に push されてビルド失敗
```
remote: Your push was rejected because it contains files larger than 10 MiB.
offending: learning_data_perfect_tier.zip
```

### 対処: クリーンデプロイ（手動）
```powershell
# 一時ディレクトリ作成
mkdir C:\Users\t-tsuchiya\Documents\keiba_hf_deploy
cd C:\Users\t-tsuchiya\Documents\keiba_hf_deploy

# 必要なファイルだけコピー（ZIPやCSVは除く）
# app.py, features_engine.py, requirements.txt
# src/ ディレクトリ（__pycache__不要）
# .streamlit/config.toml
# pedigree_master_all.csv（3.14MB, これだけCSVでOK）
# horse_memos.json

# 新規gitリポジトリとして push
git init
git branch -M main
git config user.email "action@github.com"
git config user.name "Manual Deploy"
git add .
git commit -m "Clean deploy"
git remote add hf https://ebi44323:<HF_TOKEN>@huggingface.co/spaces/ebi44323/keiba-ebye
git push --force hf main
```

---

## 環境・設定

### GitHub リポジトリ
- `https://github.com/ebi44323/keiba_ai` (main ブランチ)
- push → GitHub Actions sync.yml → HF Space 自動反映

### HF Space
- URL: `https://huggingface.co/spaces/ebi44323/keiba-ebye`
- Secrets 設定済み: `HF_TOKEN`, `HF_REPO_ID`
- Secrets 未設定（必要時に設定）: `PRO_PASSWORD`, `OWNER_KEY`, `GEMINI_API_KEY`, `DISCORD_WEBHOOK_URL`

### HF Hub モデルリポジトリ
- `ebi44323/keiba-ebye-models`（Dataset リポジトリ）
- `keiba_model.pkl` — 学習済みモデル bundle（19要素）
- `ai_daily_history.csv` — 振り返り日次成績

---

## ローカル開発環境（別PCでセットアップ）

```bash
git clone https://github.com/ebi44323/keiba_ai.git
cd keiba_ai
pip install -r requirements.txt

# 環境変数設定（PowerShell）
$env:HF_TOKEN="hf_xxxxxxxxxxxx"
$env:HF_REPO_ID="ebi44323/keiba-ebye-models"
$env:GEMINI_API_KEY="AIzaSy..."

# 起動
streamlit run app.py
```

**注意**: `learning_data_perfect_tier.zip`（35MB, Git LFS管理）はローカル学習時のみ必要。
アプリ動作だけなら不要（モデルはHF Hubからダウンロード）。

---

## 次にやること（優先度順）

### 高優先度
1. **HF Space ビルド完了確認** → `v2026-03-26d` が表示されたら全機能が反映済み
2. **PRO_PASSWORD を HF Secrets に設定**（週末レースまでに）
   - HF Space > Settings > Variables and secrets > New secret
   - `PRO_PASSWORD`: 任意のパスワード
   - `OWNER_KEY`: オーナー用URLキー（?key=でアクセス）
3. **GEMINI_API_KEY を HF Secrets に設定**（AIアナリスト機能を有効化）

### 中優先度
4. **◎選択ロジックをEV優先に変更**（CLAUDE.md の次にやること参照）
   - 現在: `◎ = max(AI勝率)`
   - 改善: `EV=AI勝率×オッズ >= 1.0 かつ AI勝率 >= 0.10` の馬を優先
   - 実装箇所: `src/inference.py` の ◎判定ロジック

### 低優先度
5. temp ディレクトリ削除: `C:\Users\t-tsuchiya\Documents\keiba_hf_deploy`
6. `HANDOVER_2026-03-26.md`（このファイル）の削除（機密情報含む場合）

---

## ファイル構成（主要）

```
app.py                     メインUI（v2026-03-26d）
src/
  gemini_utils.py          ★Gemini二人アナリスト（今回実装）
  discord_utils.py         Discord通知（Webhook直接送信）
  inference.py             推論エンジン
  core_model.py            モデル管理（HF Hub連携）
  features_engine.py       特徴量定義（50特徴量）
  config.py                定数一元管理
.streamlit/config.toml     port=7860 設定（HF必須）
.github/workflows/sync.yml GitHub→HF自動デプロイ
requirements.txt           依存パッケージ（バージョン固定）
CLAUDE.md                  プロジェクト詳細仕様書
```

---

## トラブルシューティング

| 症状 | 原因 | 対処 |
|---|---|---|
| HF "Restarting" だがアプリ動作 | ダッシュボード表示ラグ | 実態は Running、無視でOK |
| Building で No logs | ゼロダウンタイムデプロイ中 | 旧コンテナが動いている。待つ |
| push rejected (10MiB超) | ZIPがHFに送られた | sync.yml確認 / クリーンデプロイ実施 |
| キャプションが変わらない | 旧コンテナが稼働中 | ビルド完了を待つ |
| GeminiトグルがデフォルトOFF | session_state が残存 | ハードリフレッシュ or シークレットタブ |
